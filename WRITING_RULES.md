# WRITING_RULES.md — 文档输出规范（唯一权威真源）

> 本文件是仓库写作规范的**唯一权威真源**。`README.md` / `AGENTS.md` 中既有的写作约定仅作为本文件建立前的历史参考，任何冲突以本文件为准（见 [`V2_MVP_SPEC.md`](./V2_MVP_SPEC.md) §12、§13）。
> 本文件由 Agent 与用户共同遵守；它不是 Agent 行为宪法（那是 [`AGENTS.md`](./AGENTS.md)），也不是每次动作步骤（那是 [`WORKFLOW.md`](./WORKFLOW.md)）。

## 0. 应用原则（最重要）

- **按语义 / `kind` / 本次任务选择性应用**，不要机械套模板；
- **已有 canonical 结构优先**：尊重文档既有的合理结构，不强行重排；
- **新模板主要指导新文档**；对已有文档只做受约束的增量更新；
- **不存在的信息不造空章节**：某模板章节在当前文档没有对应内容时，直接省略，不要补“暂无”；
- **禁止无关的大规模格式化**：不改变语义的重排版一律不做。

---

## 1. 通用规则

### 1.1 公式

- 行内公式：`$Q(s,a)$`
- 独立公式：
  ```
  $$
  Q(s,a)=r+\gamma\max_{a'}Q(s',a')
  $$
  ```
- 使用兼容标准 Markdown 的 LaTeX；不使用只在特定编辑器有效的私有公式语法。

### 1.2 术语

术语首次出现时使用「中文名称（英文全称，必要时给出缩写）」，后文方用缩写：

> 视觉—语言—动作模型（Vision-Language-Action Model，VLA）

避免连续堆叠未解释的英文首字母缩写。

### 1.3 证据强度标签

按**语义**使用以下标签，而非机械地逐段添加：

- `[PAPER]`：论文作者报告的内容；
- `[RESULT]`：已有独立结果 / 复现；
- `[INFERENCE]`：Agent 或用户的推断（尚未验证）；
- `[OPEN]`：开放 / 未验证问题。

推荐表述：“论文报告……”“该实验支持……但不能证明……”“可以推测……，仍需通过……验证。”

### 1.4 标题与检索

- 标题应明确表达内容，便于 GitHub Search 与人工定位；
- 长文档维护目录与章节导航。

### 1.5 极简 Front Matter

知识 Markdown 顶部使用极简 Front Matter：

```yaml
---
id: <stable-id>
title: 文档标题
kind: <文档类型>
domain: <领域>
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

- 按需增加：`sources:`（来源引用）、`read_at:`（阅读 / 精读时间）等；
- `kind` 只承担写作规则路由，**不决定目录结构**；
- `id` 是稳定身份标识（[`DECISIONS.md`](./DECISIONS.md) D-004）。

---

## 2. `kind` 专项规则（指导新文档，非强制 checklist）

> 以下章节是**建议结构**，用于指导新建对应 `kind` 的文档；已有文档保持其既有结构，只做增量更新。不要为套模板而造空章节。

### 2.1 `learning-plan`（学习规划与进度）

- 目标与范围；
- 阶段 / 里程碑；
- 进度与状态；
- 重要问答与误区修正索引（指向对应长期学习笔记）。

### 2.2 `learning-note`（长期学习笔记）

- 核心概念与推导；
- 关键结论与证据强度；
- 重要问答 / 误区修正沉淀；
- 与已有知识的联系。

### 2.3 `paper-summary`（受约束论文快速总结）

- 必须标记 **`Agent-generated` / `user-unverified`**；
- 核心问题、方法概览、主要结果、局限；
- **不得写成用户个人判断**；
- 同一篇论文只此一个文件，未来演化为 `paper-note`（[`DECISIONS.md`](./DECISIONS.md) D-010）。

### 2.4 `paper-note`（用户精读后的论文笔记）

- 基本信息（论文 / 作者 / 时间 / 链接 / 代码 / 开源状态 / 核验日期）；
- 一句话定位、核心问题、方法概览、数学形式、实验与结果、特色贡献；
- 与已有工作的关系、局限、对当前研究的参考价值；
- 可迁移实验、待验证问题。

### 2.5 `topic-note`（主题 / 专题笔记）

- 问题定义；
- 技术路线与代表工作；
- 横向比较、共同假设、已知局限；
- 开放问题、对当前研究的启发；
- 资源索引。

### 2.6 `experiment`（实验记录）

- 研究问题与可证伪假设；
- 数据 / 环境 / 基线 / 指标；
- 实验矩阵与结果；
- 异常与失败、结论边界、下一步。

### 2.7 `idea`（研究想法）

区分：原始想法、合理部分、隐含前提、反例、可验证预测、最小实验、当前可信度。不要把想法直接写成结论。

### 2.8 `report`（阶段报告 / 汇报）

- 背景与目标；
- 主要结论 / 产出；
- 支撑材料与链接；
- 下一步。

### 2.9 `map`（认知结构地图 `_map.md`）

承载：主题问题、当前知识结构、关键关系、代表工作、当前判断、开放问题、认知演化、相关主题。

- **章节按语义使用，而非强制填满**；
- **认知演化 / 当前判断**只有在**用户明确表达或确认认知变化**时才写入；Git history 仅作证据，不能单独授权推断（[`DECISIONS.md`](./DECISIONS.md) D-005）；
- 仅维护事实型低风险内容（导航、链接、代表工作），不自动补写需要理解的内容。

---

## 3. 长期学习双文档模式

持续学习保留「**学习规划与进度 + 长期学习笔记**」双文档：

- 规划文档管目标 / 阶段 / 进度；
- 笔记文档沉淀概念、结论与重要问答 / 误区修正；
- 重要澄清先进入问答区，再回写笔记，避免结论漂移。

## 4. 与协议栈其它文件的关系

- 本文件只管**文档怎么写**；
- [`AGENTS.md`](./AGENTS.md) 管 Agent 行为边界与 Git 安全（**不含写作规则**）；
- [`WORKFLOW.md`](./WORKFLOW.md) 管每次 curation 的动作步骤；
- [`STATE.md`](./STATE.md) 只提供运行时上下文。
